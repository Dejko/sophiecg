class PatchSelection {

    constructor(name, patchSelection, origin) {
        this.name = name;
        this.patchSelection = patchSelection
        this.origin = origin

    }


    insertPatch() {

    }

    removePatch() {

    }

    setSelectionBounds(n) {
        //this.origin.row + n
        //this.origin.col + n
    }


    getName() {
        return toString(this.name);
    }
}